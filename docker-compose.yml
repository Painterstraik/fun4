version: "3.9"
services:
  db:
    image: postgres:15
    restart: unless-stopped
    environment:
      POSTGRES_USER: fund4
      POSTGRES_PASSWORD: fund4
      POSTGRES_DB: fund4
    ports:
      - "5432:5432"
    volumes:
      - fund4_data:/var/lib/postgresql/data

volumes:
  fund4_data:
